package com.demo.generics;

/*
* HRM System
* -> log đơn nghỉ phép
* -> log đơn OT
* -> duyệt đơn (danh sách duyệt đơn của cả phòng, chỉ trường phòng đc xem)
* -> hiển thị ra màn hình danh sách đơn nghỉ phép mà nhân đó đã log và trạng thái của nó (chờ dyệt, đang duyệt, đã duyệt, từ chối)
* -> duyệt đơn nghỉ phép của nhân viên, sẽ được trưởng phòng duyệt
*
*
*
*
*
* */

public class FloatServiceImp implements ArrayService<Float>{
    @Override
    public void printArray(Float[] array) {
        // Todo;
    }
}
